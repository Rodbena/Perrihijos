import React from 'react'
import Layout from '../../components/Layout'

/**
* @author
* @function Ordenes
**/

const Ordenes = (props) => {
  return(
    <Layout sidebar>
        Ordenes
    </Layout>
   )

 }

export default Ordenes